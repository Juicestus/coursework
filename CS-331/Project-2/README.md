# Gang 22 Project 2

**Note:** to create orders, first use the `scripts/create_orders.py` script. This generates three `data/*.csv` files that can then be ingested into the database with `\i scripts/load_orders.sql`.